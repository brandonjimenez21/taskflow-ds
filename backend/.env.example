# Configuración de la base de datos
# Cambia "password" por la contraseña de tu usuario postgres local
# Si tu base de datos se llama diferente a "taskflowds", cambia también el nombre
DATABASE_URL="postgresql://postgres:password@localhost:5432/taskflowds"

# Clave secreta para JWT (autenticación de usuarios)
# Usa una cadena larga y difícil de adivinar en producción
JWT_SECRET="supersecreto"
